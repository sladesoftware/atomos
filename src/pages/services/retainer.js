import React from "react"
import { Page, Layout, GutterContainer } from "components"

const RetainerPage = () => (
  <Page title="Services | Retainer">
    <Layout>
      <GutterContainer>
        <div>{`TODO: Retainer Page`}</div>
      </GutterContainer>
    </Layout>
  </Page>
)

export default RetainerPage